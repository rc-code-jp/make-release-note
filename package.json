{
  "name": "make-release-note",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js -o dist",
    "package": "npm run build"
  },
  "keywords": [
    "github-actions",
    "release-notes",
    "gemini-api"
  ],
  "author": "",
  "license": "ISC",
  "description": "GitHub Action to generate release notes using Gemini API",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@google/genai": "^1.8.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
